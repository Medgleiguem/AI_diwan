flask==3.0.3
flask-cors==4.0.1
google-generativeai==0.8.3
requests==2.32.3
gunicorn==23.0.0
python-dotenv==1.0.1

# Optional — install for proper Arabic prosody analysis:
# pip install git+https://github.com/ARBML/qawafi.git
# (if not installed, a rule-based fallback is used automatically)
